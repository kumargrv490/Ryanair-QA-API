package Rayanair;

import static io.restassured.RestAssured.*;
import static org.hamcrest.Matchers.*;

import java.util.HashMap;

import org.testng.annotations.Test;


public class qaAPI {

	@Test
    public void getUser() {
        given()
           
        
        .when()
            .get("http://127.0.0.1:8900/user/1")
        .then()
            .statusCode(200)
            .body("id",equalTo(1))
            .log().all();
     
	}
	
	
	@Test
        public void createUser() {
		
		
		@SuppressWarnings("rawtypes")
		HashMap<String, Comparable> data = new HashMap<String, Comparable>();
		data.put("id",6);
		data.put("name","sw");
		data.put("email","sw@example.com");
            given()
                .contentType("application/json")
                .body(data)
            .when()
                .post("http://127.0.0.1:8900/user")
            .then()
                .statusCode(201)
           
                .log().all();

	
	}	

	@Test
    public void getBooking() {
        given()
           
        
        .when()
            .get("http://127.0.0.1:8900/booking/1")
        .then()
            .statusCode(200)
            .body("id",equalTo(1))
            .log().all();
     
	}
	
	
	@Test
    public void createBooking() {
		
		
		@SuppressWarnings("rawtypes")
		HashMap<String, Comparable> data = new HashMap<String, Comparable>();
		data.put("id",9);
		data.put("date","2025-10-19");
		data.put("destingnation","dublin");
		data.put("origin", "madrid");
        given()
            .contentType("application/json")
            .body(data)
        .when()
            .post("http://127.0.0.1:8900/booking")
        .then()
            .statusCode(201)
           
            .log().all();


}	
}
